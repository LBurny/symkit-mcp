# 任务 NN — <主题>（<lane>·<难度>）

**纪律要求：不得读取任何源码目录；不得读取其他 lane 的 findings/ 或 runs/ 目录；不得向沙箱目录之外写入任何文件（包括任何"记忆"/台账）。所有符号数学必须用 MCP 工具完成，禁止心算后伪造工具结果。同一报错不重试超过两次。**

## 需求（原始用户口径）

> <一到两句用户会说的话。模糊卡：不列公式、不给可验证锚点、不指定步数。
> 针对性验证卡：可展开 (a)(b)(c)，并加"特别观察：……（历史上有 …… 先例，请复查并原样摘录）"。>

## 要求

1. 会话名 `<kebab-name>`。
2. 判定与异常字段的**原文摘录**：`verification_status` / `details` / `warning` / `reason` / `certification`，含反例。不要逐个调用 dump 完整 JSON（r18 教训：操作员为回收产物 Grep 自己转录 8 次，纯属开销）。
3. <失败时的替代路径，如：若 sqrt 失败，尝试平方后检查，记录两条路径。>
4. <不一致/异常情形必须给出原文，如 mismatch 字段原文。>

<!-- 重计算卡加护栏（按需启用）：
5. 大数值问题禁止把结果写成显式逐项分数链；单个表达式显式加项 ≤200；数值求和先给上限并说明精度策略。
-->

## 产物

写 `I:\Formulation\example\symkit-mcp-test-rNN\findings\task-NN.md`（本 lab 绝对路径，克隆后必须重指向）：统计、结果表、异常原文、缺陷分类（CRASH/WRONG/SILENT/UX/OK）、一段话评价。